import { FadeIn, FadeInStagger } from "@/components/Global/FadeIn";
import { FaRegCompass, FaCode, FaHeadset, FaRegIdCard } from "react-icons/fa";

const stats = [
  {
    // name: "Strategic Planning",
    name: "Strategizing",
    color: "sky",
    value:
      "Crafting and executing technical strategies and plans, roadmaps and architectures",
    icon: FaRegCompass,
  },
  {
    name: "Development",
    color: "blue",
    value:
      "Developing, Deploying and Integrating custom software applications or products",
    icon: FaCode,
  },
  {
    name: "Upkeep",
    color: "sky",
    value:
      // "Digital Asset and vendor management, with user training and help desk support",
      "Digital asset and vendor managing, with user training and help desk support",
    icon: FaHeadset,
  },
  {
    name: "Management",
    color: "sky",
    value:
      "Cybersecurity, and Systems administration preventing problems before they start",
    icon: FaRegIdCard,
  },
];

const StatsComponent = () => (
  <FadeInStagger faster>
    <FadeIn className="text-center">
      <h2 className="text-3xl font-extrabold text-gray-900 sm:text-4xl">
        {/* How we aim to help you succeed */}
        {/* Solve it with Halfnine */}
        Solve and Build with Halfnine
      </h2>
    </FadeIn>
    <div className="mt-8 grid grid-cols-1 gap-8 overflow-hidden sm:grid-cols-2 lg:grid-cols-4">
      {stats.map((stat) => (
        <FadeIn
          key={stat.name}
          className={`flex flex-col p-5 transition-colors duration-200 rounded-2xl border-2 gap-1 bg-white border-${stat.color}-500`}
        >
          <stat.icon
            className="h-6 w-6 text-gray-500 group-hover:text-sky-600"
            aria-hidden="true"
          />
          <dd className="text-lg font-bold text-gray-900">{stat.name}</dd>
          <dt className="text-sm leading-2 text-gray-600">{stat.value}</dt>
        </FadeIn>
      ))}
    </div>
  </FadeInStagger>
);

export default StatsComponent;
